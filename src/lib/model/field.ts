export interface Field {
    fieldId: string // an incremental id
    fieldName: string // category name
    hightPercent: number // lol, a typo!
    isBroken: boolean // why
    subscriptions: number // the number of jobs posted
}